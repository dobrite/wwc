(function() {
    'use strict';

    var root = this;

    root.define([
        'jquery',
        ],
        function( $ ) {

            describe('Application App', function () {
                it.only('should be an instance of Marionette.Application', function () {

                });


            });

        });

}).call( this );
